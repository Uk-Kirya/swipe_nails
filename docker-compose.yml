version: "3.9"
services:
  django:
    build: .
    container_name: SwipeNails
    volumes:
      - .:/app
      - ./media:/app/media
      - ./staticfiles:/app/staticfiles
    ports:
      - "8016:8000"
    restart: always
